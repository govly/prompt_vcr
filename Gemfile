source "https://rubygems.org"

# Specify your gem's dependencies in prompt_vcr.gemspec.
gemspec

gem "puma"

gem "sqlite3"

# Use sprockets instead of propshaft for Ruby 2.6 compatibility
gem "sprockets-rails"

# Use a JavaScript approach for Tailwind instead of the Rails gem
# We'll use the npm package we've already set up

# Omakase Ruby styling [https://github.com/rails/rubocop-rails-omakase/]
gem "rubocop-rails-omakase", require: false

# Start debugger with binding.b [https://github.com/ruby/debug]
# gem "debug", ">= 1.0.0"
